plugins {
  id "org.jetbrains.kotlin.jvm" version "1.3.50" apply false
}

task install {
  dependsOn ":annotations:build", ":annotations:publishToMavenLocal", ":processor:build", ":processor:publishToMavenLocal"
}
